name: widget_wall
description: Headless Windows grid launcher for desktop widgets and apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  ffi: ^2.1.2
  win32: ^5.5.4
  yaml: ^3.1.2

dev_dependencies:
  lints: ^5.1.1
  test: ^1.31.2
